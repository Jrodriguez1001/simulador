---
import Particles from "astro-particles"
import type { ISourceOptions } from "tsparticles-engine";
const options: ISourceOptions = {
    // No full screen but the container size
    fpsLimit: 60,
    background: {
        color: "#1D6FB8"
    },
    fullScreen: {
        enable: true,
        zIndex: -1
    },
    detectRetina: true, // Para mejorar la calidad en pantallas de alta resolución
    interactivity: {
        detectsOn: "canvas",
        events: {
            onHover: {
                enable: true,
                mode: "repulse"
            },
            onClick: {
                enable: true,
                mode: "push"
            },
            resize: true
        },
        modes: {
            repulse: {
                distance: 100,
                duration: 0.4
            },
            push: {
                quantity: 4
            }
        }
    },
    particles: {
        number: {
            value: 250
        },
        color: {
            value: "#ffffff"
        },
        shape: {
            type: "circle"
        },
        move: {
            enable: true
        },
        size: {
            value: 1
        },
        links: {
            enable: true,
            distance: 100,
            color: "#ffffff",
            opacity: 0.4
        },
        collisions: {
            enable: true
        },
        opacity: {
            value: 0.5
        },
        rotate: {
            value: 90,
            direction: "clockwise",
            animation: {
                enable: true,
                speed: 10
            }
        },
    }
};

---

<script>
    import { type Container, type Engine, tsParticles } from "tsparticles-engine";
    import { loadFull } from "tsparticles";

    // the function name is the parameter passed to the init attribute
    // required
    // add the function to window is mandatory, it will be searched there
    window.particlesInit = async function (engine: Engine) {
        await loadFull(engine);
    }
    
    // the function name is the parameter passed to the loaded attribute
    // optional
    // add the function to window is mandatory, it will be searched there
    window.particlesLoaded = function (container: Container) {
        console.log("particlesLoaded callback");
    }
</script>

<Particles id="tsparticles" options={options} init="particlesInit" />